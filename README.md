# Steps to create and run the my-gpg image
- Create docker image from docker file: `docker build -t gpg-img`
- Run the docker image shell: `docker run -it gpg-img`




# References
- [1] http://irtfweb.ifa.hawaii.edu/~lockhart/gpg/
